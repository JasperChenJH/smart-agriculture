<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.soultalk.mapper.AgentMapper">
    <insert id="insert">
        insert into soultalk.agent(name, creator, create_time, prompt, introduction, photo, api, is_public)
        values (#{name},#{creator},#{createTime},#{prompt},#{introduction},#{photo},#{api},#{isPublic})
    </insert>
    <select id="countById" resultType="java.lang.Integer">
        select count(*)
        from soultalk.agent
        where id=#{id}
    </select>
</mapper>